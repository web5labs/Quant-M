# Open Questions

- Is Telegram meant to stay an optional adapter, or should it become a first-class supported runtime surface?
- Should Quant-M add durable session history as a first-class runtime surface, or keep queue/state artifacts as the primary persistence model for now?
- Should a future adapter contract be formalized for external orchestrators, or should Quant-M remain intentionally standalone longer?
- How far should forex execution progress beyond shared-state persistence and paper/sandbox handoffs?
- Which deployment target is primary for the next real build slices: local desktop, VPS, Raspberry Pi, or Android worker node?
- Should the copied `Ponboarding` and `Staff-OS` repos stay embedded in `quantm`, or move to an external reference workflow later?
